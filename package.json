{
  "name": "subpool-worker",
  "version": "0.1.0",
  "description": "Sub Pool",
  "main": "src/index.js",
  "scripts": {
    "deploy": "npm run build && wrangler deploy",
    "build": "npm i && wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251127.0",
    "wrangler": "^4.32.0"
  },
  "dependencies": {
    "itty-router": "^5.0.22"
  }
}
