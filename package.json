{
  "name": "subpool-worker",
  "version": "0.1.0",
  "description": "SubPool Worker",
  "main": "src/index.js",
  "scripts": {
    "deploy": "npm run build && wrangler deploy",
    "build": "npm ci && wrangler deploy --dry-run",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "wrangler": "^4.59.1"
  },
  "dependencies": {
    "itty-router": "^5.0.23"
  }
}
