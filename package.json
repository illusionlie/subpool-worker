{
  "name": "subpool-worker",
  "version": "0.1.0",
  "description": "SubPool Worker",
  "main": "src/index.js",
  "scripts": {
    "deploy": "npm run build && wrangler deploy",
    "build": "npm ci && wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev --env dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250823.0",
    "wrangler": "^4.59.1"
  },
  "dependencies": {
    "itty-router": "^5.0.22"
  }
}
